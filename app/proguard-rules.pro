# Keep this file minimal.
# Add explicit keep rules here only when a dependency or release-only issue requires them.
# Examples: reflection-based SDKs, JSON serialization, WebView JS interfaces, dynamic class loading.
